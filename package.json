{
  "name": "facilitator",
  "version": "1.0.0",
  "description": "x402 Facilitator for micropayment channels",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run --watch index.ts",
    "start": "bun run index.ts",
    "build": "bun build index.ts --outdir ./dist --target bun",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "ethers": "^6.15.0",
    "hono": "^4.9.9",
    "zod": "^4.1.11"
  }
}
